# Supabase / Lovable Cloud – ohne diese Werte läuft die Seite im Demo-Modus
# (Seed-Daten aus src/data/couches.ts, Bestellungen in .data/demo-store.json).

# Client (Browser) – anon key ist öffentlich, RLS schützt die Daten
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Server – service role NUR serverseitig, niemals mit VITE_ Präfix!
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
